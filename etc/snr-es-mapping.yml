---
mappings:
  document:
    _source:
      enabled: true
    _timestamp:
      enabled: true
      store: true

    dynamic_templates:
    - all_fields:
        match: "*"
        mapping:
          index: "no"
